import * as React from 'react';
import { Card, CardBody } from 'reactstrap';

const LaunchShaderOptions: React.FC = () => {
    return <>
        <Card>
            <CardBody>
                Coming soon - Launch Shader Options
            </CardBody>
        </Card>
    </>;
};

export default LaunchShaderOptions;
